# @file dashboards.yml
# @brief Указывает Grafana, откуда брать JSON-файлы дашбордов.

apiVersion: 1
providers:
  - name: 'NFS Dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
